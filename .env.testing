APP_NAME=FacturX
APP_ENV=testing
APP_KEY=base64:SGVsbG9Xb3JsZEhlbGxvV29ybGRIZWxsb1dvcmxkSGVsbG9Xb3JsZA==
APP_DEBUG=true
APP_TIMEZONE=Europe/Paris
APP_URL=http://localhost

APP_LOCALE=fr
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=fr_FR

APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

LOG_CHANNEL=single
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Base de données pour les tests (SQLite en mémoire)
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# Cache pour les tests
CACHE_STORE=array
CACHE_PREFIX=

# Files d'attente pour les tests
QUEUE_CONNECTION=sync

# Sessions pour les tests
SESSION_DRIVER=array
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Redis (désactivé pour les tests)
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail pour les tests
MAIL_MAILER=array
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Services externes (mocks pour les tests)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Broadcast (désactivé pour les tests)
BROADCAST_CONNECTION=log

# Vite (dev assets)
VITE_APP_NAME="${APP_NAME}"

# Monitoring (désactivé pour les tests)
PULSE_ENABLED=false
TELESCOPE_ENABLED=false

# Security
BCRYPT_ROUNDS=4

# Facturation électronique (valeurs de test)
FACTURE_ELECTRONIQUE_ENABLED=true
PDP_PROVIDER=test
PDP_API_URL=http://localhost/test
PDP_API_KEY=test_key

# API externes (mocks)
ANNUAIRE_PPF_API_URL=http://localhost/mock
ANNUAIRE_PPF_API_KEY=test_key

# Limites pour les tests
DEFAULT_PLAN_CLIENT_LIMIT=50
DEFAULT_PLAN_INVOICE_LIMIT=50
